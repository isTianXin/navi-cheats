% git, code

#删除本地分支
git branch -d <branch>

#强制删除本地分支
git branch -D <branch>

#删除远程分支
git push origin --delete <branch>

#删除追踪分支
git branch --delete --remotes <remote>/<branch>

#根据关键词批量删除本地分支
git branch | grep '<key_word>' | xargs git branch -D

$branch: git branch | awk '{print $NF}'

