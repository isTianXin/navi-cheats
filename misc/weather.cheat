% weather

# 显示本地天气信息 get weather info of current location
curl -s "wttr.in?lang=zh"

# 显示本地当前天气信息 get current weather info of current location
curl -s "wttr.in?lang=zh&n0"

# 通过特定参数显示天气信息 get weather info by query parameters
curl -s "wttr.in/<query>"

